/// Project loading and persistence
/// 
/// This module will handle project file I/O and persistence

// Placeholder for now - will implement later
pub struct ProjectLoader;

impl ProjectLoader {
    pub fn new() -> Self {
        Self
    }
}

pub mod autosave;
